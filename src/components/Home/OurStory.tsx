import { component$ } from "@builder.io/qwik";

export const OurStory = component$(() => (
  <div class="flex flex-col h-screen justify-center items-center my-10">
    <h2 class="text-5xl font-bold">Our Story</h2>
  </div>
));
